<h1><%= @pet.name %></h1>
<p>Address: <%= @pet.address %></p>
<p>Species: <%= @pet.species %></p>
<p><%= @pet.found_days_ago %> days ago</p>

<%= link_to 'Back to index', pets_path %> |
<%= link_to 'Edit', edit_pet_path(@pet) %> |
<%= link_to 'Remove this pet', pet_path(@pet), data: { turbo_method: 'delete', turbo_confirm: 'Are you sure?' }  %>
